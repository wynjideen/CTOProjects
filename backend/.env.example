# Connection string routed through PgBouncer or a pooled endpoint
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ctoprojects?schema=public&connection_limit=1&pool_timeout=30&pgbouncer=true"

# Direct connection string that bypasses PgBouncer for administrative tasks
DIRECT_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ctoprojects?schema=public"

# Shadow database used by Prisma migrate and automated tests
SHADOW_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ctoprojects_shadow?schema=public"
